# Core - PPE Tracker runtime
opencv-python>=4.10
numpy>=2.0
python-dotenv>=1.0

# Inference backend (local, in-process — no external server)
# I detector open-vocabulary (Grounding DINO / OmDet-Turbo) sono nativi in
# transformers (no trust_remote_code) e Apache 2.0.
# NESSUNA dipendenza Ultralytics/YOLO (AGPL — incompatibile con codice
# proprietario in produzione).
torch>=2.2
torchvision>=0.17
transformers>=4.46
pillow>=10.0
timm>=1.0            # backbone Swin per omdet-turbo (Apache 2.0)

# Web UI (FastAPI, MIT) — app browser per drag-and-drop del video e tracking
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9   # upload file multipart

# Dataset download (opzionale, solo per valutazione)
# pip install roboflow

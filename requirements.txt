# Core - PPE Tracker runtime
opencv-python>=4.10
numpy>=2.0
onnxruntime>=1.18
python-dotenv>=1.0

# VLM escalation (local, in-process — no external server)
# SmolVLM è supportato nativamente da transformers (no trust_remote_code).
torch>=2.2
torchvision>=0.17
transformers>=4.46
pillow>=10.0

# YOLO ONNX backend (local package)
-e git+https://github.com/AndreaDAmicis22/yolo#egg=yolo_yolo_onnx

# Training & dataset (install separately when needed)
# pip install ultralytics roboflow
